[project]
name = "muq-tv-epg"
version = "0.1.0"
description = "Fetch EPG data from API and convert to XMLTV format."
requires-python = ">=3.11"
dependencies = [
    "requests",
    "lxml",
]

[tool.uv]
